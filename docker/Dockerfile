FROM mrcide/naomi_base:latest

ARG GIT_SHA='UNKNOWN'
ARG GIT_BRANCH='UNKNOWN'
ARG NAOMI.CORE_VERSION='UNKNOWN'
ENV NAOMI.CORE_GIT_HASH $GIT_SHA
ENV NAOMI.CORE_GIT_BRANCH $GIT_BRANCH
ENV NAOMI.CORE_VERSION $NAOMI_VERSION

COPY . /src
